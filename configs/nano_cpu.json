{
  "_comment": "Smallest useful run. Trains in ~30s on a laptop CPU. Use with: --preset gpt2-nano --n_layer 4",
  "data_dir": "data/tinyshakespeare_char",
  "out_dir": "out/nano",
  "batch_size": 16,
  "block_size": 64,
  "max_steps": 600,
  "warmup_steps": 50,
  "learning_rate": 0.003,
  "eval_interval": 150,
  "eval_iters": 20,
  "log_interval": 100,
  "device": "cpu",
  "dtype": "float32",
  "strategy": "single",
  "init_from": "scratch"
}
